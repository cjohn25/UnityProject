using UnityEngine;

public class Gun2D : MonoBehaviour
{
   //[SerializeField] private Transform weaponMountPoint;
   // private float FireSpeed = 300f;
   // [SerializeField] private GameObject bulletPrefab;
    // Start is c
    // alled once before the first execution of Update after the MonoBehaviour is created
    public void Update()
    {
        
    }
    public void Fire()
    {
        
            //Debug.Log("Fire");
            //var bullet = Instantiate(bulletPrefab, weaponMountPoint.position, weaponMountPoint.rotation);
            //bullet.GetComponent<Rigidbody2D>().linearVelocity = weaponMountPoint.up * FireSpeed;

      
    }
}
